int mdc(int n, int m){
    int r;

    if (m % n == 0) {
        return r = n;
    } else if (n % m == 0) {
        return r = m
    } else if (m > n) {
        mdc(n, m % n, r);
    } else {
        mdc(m, n % m, r);
    }
}

int main(){
    int r = mdc(3, 6);
    print(r);
};
