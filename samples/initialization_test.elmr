int main(){

int x = 10;        // Inicialização válida
float y = 3.14;    // Inicialização válida
z = 5;             // Erro: variável 'z' não declarada
char x = 'a';      // Erro: variável 'x' redeclarada no escopo global
float y = "test";  // Erro: tipos incompatíveis na inicialização
};
